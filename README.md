# aetherunbound.net

## Installation & Building

### For development

1. `npm install`
2. `npm watch` in one terminal, and `npm serve` in another

### To build for publishing

1. `npm install`
2. `npm generate`
